var count = 0;
$(document).ready(function() {
	$('#input').keydown(function() {
		$("#input").bind("keydown", function() {
			var text = $(this).val();
			if (text.length != 0) {
				$("#badge").append(count++)
			});
		}
	});
});